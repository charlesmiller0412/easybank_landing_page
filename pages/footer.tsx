import type { NextPage } from "next";
import Image from "next/image";

const Footer: NextPage = () => {
    return <footer className="bg-dark-blue h-[510px]">footer</footer>;
};

export default Footer;
